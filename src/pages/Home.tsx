import MovieList from "@/features/movies/components/MovieList";

const Home = () => {
  return (
    <section className="tablet:py-5">
      <MovieList />
    </section>
  );
};

export default Home;
